services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: apps/api/.env
    environment:
      NODE_ENV: production
      PORT: "80"
    ports:
      - "3000:80"
    networks:
      - internal

networks:
  internal:
    driver: bridge
