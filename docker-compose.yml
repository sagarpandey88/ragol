services:
  app:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    env_file: apps/api/.env
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
    networks:
      - internal

networks:
  internal:
    driver: bridge
